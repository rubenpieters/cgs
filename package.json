{
  "name": "cgs",
  "version": "0.0.1",
  "description": "cgs",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "uws": "^9.14.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:cl": "pulp build -O -m ClientMain --to dist/js/purs.js",
    "build:sv": "pulp build -O --to index.js",
    "build": "npm run build:cl && npm run build:sv"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rubenpieters/cgs.git"
  },
  "author": "rubenpieters",
  "homepage": "https://github.com/rubenpieters/cgs#readme"
}
