{
  "name": "cgs",
  "version": "0.0.1",
  "description": "cgs",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "express": "^4.16.2",
    "uws": "^9.14.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "pulp test",
    "build:cl": "pulp build -O -m ClientMain --to dist/js/purs.js",
    "build:sv": "pulp build -O --to index.js",
    "build": "npm run-script build:cl && npm run-script build:sv",
    "heroku-prebuild": "npm install -g bower purescript pulp && bower install && npm run-script build:sv"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rubenpieters/cgs.git"
  },
  "author": "rubenpieters",
  "homepage": "https://github.com/rubenpieters/cgs#readme"
}
